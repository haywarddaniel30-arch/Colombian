import React from 'react';

const ThemeToggle = () => {
    return (
        <button onClick={() => { /* toggle theme logic */ }}>
            Toggle Theme
        </button>
    );
};

export default ThemeToggle;
